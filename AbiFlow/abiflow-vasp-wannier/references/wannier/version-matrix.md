# Wannier90 Version Matrix

| version_prefix | features | cautions |
| --- | --- | --- |
| 3.1 | stable-disentanglement | Prefer the documented option names for 3.x and still validate interpolation instead of trusting syntax alone. |
| 3.0 | stable-disentanglement | Treat workflows as broadly compatible with 3.1, but confirm optional post-processing expectations manually. |
| 2.1 | legacy-defaults | Legacy setups may rely on older defaults, so ask before assuming a modern template applies unchanged. |

Cross-software behavior belongs in `references/version-combinations.md`, not in this per-software table.
